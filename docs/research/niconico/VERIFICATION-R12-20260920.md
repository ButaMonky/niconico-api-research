# R12検証記録

2026-09-20。本体・Zenza固有コード変更なし。外部コードを実行せず、固定4資料と既存匿名化集計を読み取り確認。新規ニコニコ通信0、コメント取得/投稿・キー発行も0。

過去100コメント要求のHTTP/本文200、延べ38,635要素、空17、Web相当text/plain76・Cookieなし76、iOS相当application/json24・Cookieあり24を再集計。全100にキー存在、38watchモデルにキー存在、応答thread数3×93/4×7、voltageZone項目55件。生キャプチャ・実キー・本文・userId・thread実IDを再出力していない。

公開前に形式・出典hash、ローカルリンク、Git差分/未追跡/生成物、共有ZIPのmanifestを確認する。公開対象は独自解析・集計・固定URL/hashのみで、参照外部コードやローカル絶対パス付き開発指示文は除外する。現在実サイトで動作したという検証ではない。

## 実施結果

- 21 findings / 21 sourcesの形式・出典hash検証成功。
- 公開89ファイル・ZIP内89ファイル＋manifestの整合性、相対リンク、秘密情報パターン検査成功。
- 手動確認：固定ソースの要求組立・保存変換箇所、Gistとの階層差、集計値の分母、READMEと差分・新規6ファイルを確認。100保存成功と本日の再現を分離した。
- userIdのフィールド出現回数83が実ID表記と紛らわしかったため、field_name/response_countの配列に変更。値はコメント投稿者の実IDではない。検査を弱めず、出典hashとZIPを再生成して通過した。
- 文書・集計だけの追加なので、新しい実装テストや外部コード実行は行っていない。既存診断9テストの前回成功をR12のAPI実測結果とは扱わない。
- 共有先の具体的な場所・別開発向け指示文はローカル共有資料のみ。GitHubには公開用の解析結果だけを反映する。

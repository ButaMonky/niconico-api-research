# NG向け研究引き渡しの検証記録

2026-09-20 JST。対象R02〜R08。公開範囲は研究選択版で、本体変更・生記録・外部コード丸ごとの複製を含めない。

## 確認範囲

- R02：5 contributionの6観察をREPORTEDとして保存。提供資料hashは入力識別用で、原通信の真偽を独立保証しない。
- R03：固定commitのOSS2組を確認。ニコニコへ4 GET（CSV失敗1、単独2、反復1）。認証なし。診断の匿名化/停止をオフラインテストで確認。
- R04：新着2動画、version前後・検索・snapshot・単独2の6 GET。snapshot欠落は一致比較不能としてnullで保存。
- R05：公開HTMLの構造探索1＋匿名化集計1 GET。32 owner、tag/tags0。実ブラウザ未確認。
- R06：公式公開JS3ファイルの静的読み取り。ページJavaScriptを実行したものではない。
- R07：ブラウザ安全ポリシーによる拒否を記録。未検証をFAILED-LIVEやCORS不可にしない。
- R08：NG現行commitの読み取りと合成入力のみ。hiddenの拒否・詳細完了前の投稿者ルール保留を確認。本体修正なし。

今回新規のニコニコ情報取得は12 GET、別途公式静的asset3 GET。R00の既存8 GETは今回の新規数に含めない。外部OSSの参照取得は別。自動の大量通信・アカウント操作・コメント投稿なし。

## ローカル検査

- validate.py：10 findings / 11 evidence sourcesのschema・参照・SHA256。
- export.py：選択版ZIPとmanifestを生成し、収録全ファイルのhash一致を検査。
- テキスト、差分、生成ZIP：秘密値・個人パス・内部URL・不要な投稿者値を機械検査し、主要な結論/出典/失敗/未確認は本文も確認。
- 相対リンクとタスク状態：R02/R03/R04/R08は調査単位完了。R05/R06は確認範囲をHTML/静的に限定して終了。R07はBLOCKED。後回しの項目は未完了のまま。
- 研究診断の4テスト成功。最初の一括テストでは既存snapshotテストの一時フォルダ権限で失敗し、制限外の通常権限によるオフライン再実行で4件成功した。製品の挙動を変える修正はしていない。

ローカル検査はAPI仕様の将来保証でも、未知の秘密値が絶対にない証明でもない。公開動画IDを研究の再照合用に一部残し、完全匿名化とは呼ばない。本文hashから非配布の生応答を再計算できるわけではない。

## 引き渡しと公開

入口は[実装引き渡し](IMPLEMENTATION-HANDOFF.md)、単位別の次指示は[NEXT-TASK-PROMPTS](NEXT-TASK-PROMPTS.md)。ローカル正本へは旧資料を維持して追加し、共有には今回の選択版ZIPを使う。過去全体の自動exportをそのまま共有しない。

GitHub反映は利用者の指示により最後にまとめる。非公開PRへの保存と一般公開は別。今回、公開設定や利用許諾を変更しない。製品userscriptには既存のMIT記載があるが、新規研究資料全体の許諾選択を自動で確定するものではない。

## 最終レビューの修正

独立レビューでCriticalなし、Important1件、Minor1件。診断がwatchId欠落等の壊れたitemsでも継続する問題を、失敗する合成試験で再現して修正した。正常な部分返却・video nullは別として保持する。現行コード台帳へautofill/legacy-controller.jsのhashを追加。修正後の研究テストは6件成功。保存済みの実通信集計は変更していない。

## 公開先変更の追記

上記の非公開保存方針は研究終了時点の履歴。その後、利用者が独立した公開リポジトリを指定したため、監査済み選択版のみを新しい履歴へ移す。既存製品リポジトリの公開設定は維持し、製品コードと未監査資料は移さない。

移行先でも10 findings / 11 sourcesの参照hash、49収録ファイルとmanifest、相対リンク、秘密情報候補、研究診断6テストを確認した。追加のニコニコAPI通信は行っていない。ルートREADMEからAPI一覧・実装引き渡し・外部資料台帳へ案内する。

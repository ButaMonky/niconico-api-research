# 変更履歴

## 2026-09-20 R02 提供metadata報告

- 5件のcontributionと2件の参照要約を確認。公開用に6観察へ整理しREPORTEDで保存。原通信の独立確認は0。
- 広告100指定62返却、検索境界の条件、owner欠落と退会の非同一性を明記。元の強い見出しを条件付き判断へ限定。
- R02を完了とし、R03向け指示文を保存。GitHubと指定共有先への反映は保留。

## 2026-09-20 タスク分割と公開準備

- 既存の100件境界検証をR00とし、後続作業をR01〜R19へ分割。優先順位、推奨思考レベル、前提、成果物、終了条件を整理した。
- 公開資料ガイドと現状確認を追加。第三者向け入口、証拠区分、認証値・個人情報、出典・利用許諾、一般公開と非公開PRの違いを明記。
- この回の新規API通信は0、製品コード変更なし。今回分のremote反映・指定正本/共有フォルダ同期は未実施。ローカル成果と反映済み成果を区別する。

## 2026-09-20

- 指定100動画のタグ・投稿者を1 GETで全返却。100/101ページ上限と101 IDフィルターを分離して記録。
- 認証なし8 GETの匿名化集計、3 findings、3 sources、公開資料台帳と固定commit、最大8 GETの再現コードを追加。
- 既存の36件成功・鮮度差と提供資料を先に確認し、同じ検索比較を重複実施していない。
- 既存GitHub masterを基準に、この研究単位だけを公開対象へ選択。過去ローカルの本体変更と未監査資料は含めない。正本には過去履歴を保持して追記する。

## 2026-09-20 R03と優先方針変更

- OSS2組を固定commitで確認。複数取得は各IDへの個別要求。
- /v1/videosを認証なし4 GETで比較：CSV2件400、単独成功、反復2件は後方1件だけ。静的な一括候補を条件付き失敗へ更新。
- 秘密値を出さない再現診断と停止/匿名化テストを追加。
- 利用者が今回Astra高を許可。NG実装に直結する解析を優先し、GitHubは最後にまとめる。

## 2026-09-20 R04 新着の索引欠落

- 新着公開2動画を認証なし6 GETで照合。snapshot0件、getthumbinfoはタグ5/6件とuser_idを返し、検索ownerと2/2一致。
- 欠落は空タグ/投稿者なし/NG非該当ではない。最新watchタグ一致は未検証のまま保持。
- 応答サイズ・時刻・比較不能の扱い・最小再現手順を追加。R04の1条件を終了。

## 2026-09-20 R05 初期HTML

- 公開検索の初期HTMLに32 owner ID・タグなし。構造探索と測定で2 GET、動画情報の追加API0。
- 初期HTMLと読み込み後DOMを区別。実ブラウザ操作はサイト安全ポリシーで拒否、回避なし。R05は初期HTMLの確認と不足条件の記録で終了。

## 2026-09-20 R06 検索loader

- 現在参照された公開JS3ファイルを固定hashで静的確認。タグ検索はBiへ直接接続し初期metaを消費。汎用cache factoryは別。
- pageKey/世代/videoIdによる誤適用防止を設計条件として分離。実ブラウザ遷移は未検証。

## 2026-09-20 R07保留・R08引き渡し

- R07はブラウザ安全ポリシーによる未検証として明示し、Pythonや静的コードの成功をCORS成功としない。
- 現行NGコードを読み、合成入力でhidden正規化の拒否と投稿者ルールのthumbInfoDone依存を確認。本体変更なし。
- 既存機能を踏まえた取得順序・項目別準備状態・100動画通信比較・lock情報不足・開発検証項目をまとめた。

## 2026-09-20 公開先の分離

利用者の指定により、監査済み選択版を独立したniconico-api-researchへ移す。NG製品リポジトリの公開設定と履歴は移さない。README、収録範囲、検証方法、秘密値を持ち込まない更新方法を整備。GitHub Pagesは作らない。

## 2026-09-20 R09：ownerモデルのクライアント比較

過去Web101件・iOS387件のhidden例を項目限定で再集計。ID／種別／表示名を分ける解釈条件、Android静的範囲の制約、OSS型のnullable不一致を記録。新規ニコニコ通信0、本体変更0。資料内の実装指示を別work専用と明示した。旧観測を新しい実通信として更新していない。

## 2026-09-20 R11・R20

広告80実測・100部分返却報告・600/700報告を比較し、原通信不足とURL長訂正を保存。公開Zenza版の既取得metadata・日時更新を新規調査。合成実行で古い内容の期限判定条件を確認、匿名キーなしタグGETは400。新規ニコニコ通信1、本体変更0。次指示をR21へ更新。利用者ローカルのZenza版へ結果を一般化しない。

## 2026-09-20 R21

POST playlist/requestが認証なしで2指定2返却。追加の逆順POSTとthumbinfo2GETで投稿者ID/type2件一致。タグなし、上限・副作用未確認。新規通信4、本体変更0。長期方針をNG限定からニコニコ全体の再利用資料へ拡充、過去成果は段階的に監査公開する。

## 2026-09-20 R22

playlist/requestへ20→100の2 POST、全件返却・owner IDありを確認。全件visible/user、tag系フィールドなし。最大値や100件の全面的な正しさを断定せず、R21の独立2件照合と区別。過去成果との比較を追加、本体変更0。

## 2026-09-20 — R23〜R25

新着補完、channel ID表記、400全体失敗・重複・200部分返却を追加。新規17通信（GET10/POST7、200は11、400は3、406は3）。対照失敗時に主観測を失う診断を修正し、合成2テストで検証。Accept変更だけで406が解決するという仮説は支持されず、原因未解決を保存。NGの段階的更新着手に十分と判断し、未検証条件を別記。本体変更0。

## 2026-09-20 — R12コメント取得構造

COMMENTS-READを追加。旧100成功の再集計、Web/iOS Content-Type差、voltageZone項目55応答、OSS保存形式とAPI生構造の区別。固定commit/revisionとhashを登録し、旧未固定参照を訂正可能な形で追記。新規ニコニコ通信0、キー実値保存0、本体/Zenza変更0。別開発タスク向け指示文をローカル共有先へ保存（個人パスを含むため公開版には収録しない）。

## 2026-09-20 — R13過去ログ境界

既存100要求のadditionals空を新確認。過去ログ成功への一般化を防ぐ追補。固定nndownload/niconicojsの実コードを偽HTTPで実行し、同秒境界2種×2実装の4条件を保存。診断2本・匿名化証拠2件・findings2件を追加。キー実値保存0、ニコニコ通信0、本体変更0。

## 2026-09-20 R14

投稿キー・要求・戻り値・認証条件を固定コードから分離。共通HTTPまで含む8合成条件/仮想12要求で再試行とエラー経路を確認。COMMENTS-POST、証拠2、finding2、オフライン診断1を追加。R12/R13を現行投稿の根拠として流用しない。旧記述は保持し、新規ニコニコ通信・投稿0。

## 2026-09-20 R15-A

固定niconicomments0.4.1の単一コメント境界を追加。時刻切捨て、固定timeline300/衝突281、nakaの登録/矩形可視差を7合成条件で確認。資料1・検証1・診断1・証拠2・finding2を追加。R15全体を完了扱いにせず、次Bを記録。API通信/実投稿/本体変更0。

## 2026-09-20 R15-B

Aの2件重なり仮説を、同一layer/寸法の6合成条件で確認。281cs差の19刻み重なり、280cs別段、300cs共存なし。Aの未試験記述は履歴として保持しB追補で範囲を更新。新しい証拠/finding/診断/検証記録を各1追加。ニコニコ通信・実投稿・製品変更0。

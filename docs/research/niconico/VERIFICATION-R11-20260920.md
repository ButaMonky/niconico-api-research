# R11・R20の検証記録

確認日：2026-09-20 JST。研究資料・診断のみ。本体の変更なし。

- `tools/validate.py`：14 findings / 14 sources、参照とSHA256一致。
- `tools/probe_zenza_cache.py <local-zenza-git-checkout>`：固定2ファイルのhash一致、7日前・24時間TTLの合成試験でbefore_hit=false / after_hit=true、タグ内容不変、書込1回、欠落null。
- 公開Zenzaソース台帳：本家1ファイルと派生版19ファイル、commit・ファイルhash・URLを保持。外部コード全体は配布しない。
- `tools/export.py`：61ファイルとBUNDLE-MANIFESTを生成。ZIP整合性・各memberのSHA256を検証。
- 文書リンク、公開対象本文・差分・未追跡追加物・ZIPを点検。ローカル絶対パス、メール、Cookie/認証値、JWT、一時キー実値、不要な個人識別値、拡張内部URL等の候補検査を実施。説明用フィールド名は値と区別した。
- 一度の独立レビューで、過去80件実測と100/600/700報告、コードと合成実行と実通信の分離を確認。重大な指摘なし。

新規ニコニコ通信はタグGETの1回だけ。匿名・編集キーなし400を記録し再試行なし。広告の大量再通信、ユーザーキャッシュ読出し、投稿・削除を行っていない。

限界：形式・hash・候補パターンの検査は事実や秘密情報不存在の包括的証明ではない。独立レビューは非公開原通信を再検証していない。実導入版・ブラウザ実行・一般会員の認証条件・キャッシュ鮮度・広告上限は未確認。mainへの反映状態はGitHubのPRを参照する。
